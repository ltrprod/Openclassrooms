fonctionnement d'un switch:

recoit une trame
detecte de quel port ca vient
detecte vers quelle adresse ca va
associe l'adresse et le port si adresse connue
si adresse pas connue, envois la trame a tous les ports actifs
?? les mauvais ports renvoient une reponse negative / ne renvoient rien ??




Hub ( renvoit les trames a tout le monde) -> concentrateur
switch ( renvoit les trames uniquement au destinataire [si son adresse est connue]) -> commutateur
routeur ( switch avec un port supplémentaire pour le connecter a un modem)
